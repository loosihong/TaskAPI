spring:
  test:
    database:
      replace: NONE
  datasource:
    hikari:
      connection-timeout: 60000
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect